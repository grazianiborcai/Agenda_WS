<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:security="http://www.springframework.org/schema/security"
    xsi:schemaLocation="
    	http://www.springframework.org/schema/beans
    	http://www.springframework.org/schema/beans/spring-beans.xsd

        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd">

        
    <!-- Debugging   
	<security:debug />  --> 



	<!-- Stateless RESTful services use BASIC authentication --> 
    <security:http create-session="stateless" pattern="/**">
        <security:intercept-url pattern="/**" access="isAuthenticated()" />
        <security:http-basic />
        <csrf disabled="true"/>
        <custom-filter before="BASIC_AUTH_FILTER" ref="authFilter" />
    </security:http>
    
    
    <!-- TODO: habilitar CSRF protection 
    "isAuthenticated()"		"hasRole('ROLE_USER')"	
    FORM_LOGIN_FILTER		BASIC_AUTH_FILTER	-->
    
    
    
    <!-- Filter Bean -->
	<beans:bean id="authFilter"
		class="br.com.gda.servlet.authUser.AuthFilter">
		<beans:constructor-arg ref="authenticationManager"/>
	</beans:bean> 


	<!-- Authentication -->
    <security:authentication-manager alias="authenticationManager">
        <authentication-provider ref="authProvider" >
        </authentication-provider>
    </security:authentication-manager>
    
    
    <beans:bean id="authProvider" class="br.com.gda.servlet.authUser.AuthProvider"/>
 

    
    <!--
   	<beans:bean id="passwordEncoder" class="org.springframework.security.crypto.password.NoOpPasswordEncoder"
		factory-method="getInstance" />
   
   
    <security:authentication-manager alias="authenticationManager">
        <security:authentication-provider>
            <security:user-service>
                <security:user name="rest" password="rest" authorities="ROLE_USER"/>
            </security:user-service>
            <password-encoder ref="passwordEncoder" />
        </security:authentication-provider>
    </security:authentication-manager>
    -->


</beans:beans>